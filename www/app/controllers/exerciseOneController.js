(function(angular) {
  angular
    .module("application")
    .controller("exerciseOneController", function($routeParams, es) { // inject
      this.es = es; // assigning es to the controller as a member








    });
}(window.angular));
